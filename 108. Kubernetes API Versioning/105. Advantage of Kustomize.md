# Advantage of Kustomize

What is one advantage of using Kustomize over Helm?

Choose the correct answer from below:

Kustomize supports templated YAML.

Kustomize doesn’t use templates, making it easier to work with plain YAML files.

Kustomize allows for cluster-wide resource management.

Kustomize is only for managing storage resources.
