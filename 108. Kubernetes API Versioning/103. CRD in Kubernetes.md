# CRD in Kubernetes

What is a Custom Resource Definition (CRD) in Kubernetes?

Choose the correct answer from below:

A method to define custom roles in Kubernetes.

A way to extend Kubernetes by adding new resource types.

A command to delete resources.

A standard Kubernetes component.
