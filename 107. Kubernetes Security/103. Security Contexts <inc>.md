# Security Contexts

Objective:

The DevOps team needs to enforce security contexts for pods to ensure they run with limited privileges.

TASK -1:

Create a Pod in the default namespace with the following security context. The Pod named secure-nginx-pod should use the nginxinc/nginx-unprivileged:latest image. Set the security context to disable privilege escalation and ensure the container runs as a non-root user.

TASK - 2:

Verify that the Pod is running with the specified security context by checking the Pod's details.
